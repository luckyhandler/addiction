name: addiction
description: A calculation app for kids

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bloc: ^6.1.3
  flutter_bloc: ^6.1.3
  equatable: ^1.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Static Code Analysis
  effective_dart: ^1.3.0
  analyzer: ^0.40.0

flutter:
  uses-material-design: true
